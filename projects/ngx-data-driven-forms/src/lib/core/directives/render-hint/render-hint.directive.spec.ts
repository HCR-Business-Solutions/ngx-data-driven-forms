import { RenderHintDirective } from './render-hint.directive';

describe('RenderHintDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderHintDirective();
    expect(directive).toBeTruthy();
  });
});
