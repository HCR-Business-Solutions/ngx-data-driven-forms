import { RenderQuestionDirective } from './render-question.directive';

describe('RenderQuestionDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderQuestionDirective();
    expect(directive).toBeTruthy();
  });
});
