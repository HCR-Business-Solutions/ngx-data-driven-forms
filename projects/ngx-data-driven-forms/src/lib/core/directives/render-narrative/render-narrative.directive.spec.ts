import { RenderNarrativeDirective } from './render-narrative.directive';

describe('RenderNarrativeDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderNarrativeDirective();
    expect(directive).toBeTruthy();
  });
});
