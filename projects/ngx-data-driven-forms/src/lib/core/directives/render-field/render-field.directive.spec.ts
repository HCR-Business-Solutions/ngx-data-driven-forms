import { RenderFieldDirective } from './render-field.directive';

describe('RenderFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderFieldDirective();
    expect(directive).toBeTruthy();
  });
});
