import { RenderLabelDirective } from './render-label.directive';

describe('RenderLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderLabelDirective();
    expect(directive).toBeTruthy();
  });
});
