import { RenderRepeatInputDirective } from './render-repeat-input.directive';

describe('RenderRepeatInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderRepeatInputDirective();
    expect(directive).toBeTruthy();
  });
});
