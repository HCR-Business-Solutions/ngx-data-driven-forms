import { RenderHeadingDirective } from './render-heading.directive';

describe('RenderHeadingDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderHeadingDirective();
    expect(directive).toBeTruthy();
  });
});
