import { RenderPageDirective } from './render-page.directive';

describe('RenderPageDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderPageDirective();
    expect(directive).toBeTruthy();
  });
});
