import { RenderApplicationDirective } from './render-application.directive';

describe('RenderApplicationDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderApplicationDirective();
    expect(directive).toBeTruthy();
  });
});
