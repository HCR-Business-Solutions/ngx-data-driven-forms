import { RenderRepeatDataDirective } from './render-repeat-data.directive';

describe('RenderRepeatDataDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderRepeatDataDirective();
    expect(directive).toBeTruthy();
  });
});
