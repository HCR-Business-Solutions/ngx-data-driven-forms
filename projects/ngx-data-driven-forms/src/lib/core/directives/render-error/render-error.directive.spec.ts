import { RenderErrorDirective } from './render-error.directive';

describe('RenderErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderErrorDirective();
    expect(directive).toBeTruthy();
  });
});
