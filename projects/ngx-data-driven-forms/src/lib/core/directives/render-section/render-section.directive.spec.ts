import { RenderSectionDirective } from './render-section.directive';

describe('RenderSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderSectionDirective();
    expect(directive).toBeTruthy();
  });
});
