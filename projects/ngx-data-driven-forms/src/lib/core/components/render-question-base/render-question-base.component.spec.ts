import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RenderQuestionBaseComponent } from './render-question-base.component';

describe('RenderQuestionBaseComponent', () => {
  // let component: RenderQuestionBaseComponent;
  // let fixture: ComponentFixture<RenderQuestionBaseComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [RenderQuestionBaseComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(RenderQuestionBaseComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
    // expect(component).toBeTruthy();
  });
});
